export enum Inputs {
  UseCache = "use-cache",
  Prefix = "prefix",
}

export enum Outputs {
  CacheHit = "cache-hit",
}

export enum State {
  CachePrimaryKey = "CACHE_KEY",
  CacheMatchedKey = "CACHE_RESULT",
}
